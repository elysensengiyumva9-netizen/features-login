services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
      POSTGRES_DB: webappdb
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
